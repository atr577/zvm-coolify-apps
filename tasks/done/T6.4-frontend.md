# T6.4: Frontend

**Status:** done
**Priority:** P0
**Parent:** T6
**Depends on:** T6.3
**Created:** 2026-01-11
**Estimate:** 4ч

## Цель

Обновить frontend на новый API, удалить legacy компоненты.

## Предусловия

- [ ] T6.3 завершён (API готов)
- [ ] Backend запущен и работает

## Чеклист

### 1. Обновить api.ts

```typescript
// Удалить:
startWorkflow, approveStepV2, rejectStep, retryStep, rollbackToStep

// Добавить:
runWorkflow: (videoId) => api.post(`/api/workflow/${videoId}/run`)
generateStep: (videoId, step, feedback?) => api.post(`/api/workflow/${videoId}/generate/${step}`, { feedback })
getHistory: (videoId, step) => api.get(`/api/workflow/${videoId}/history/${step}`)
selectVariant: (videoId, variantId) => api.post(`/api/workflow/${videoId}/select/${variantId}`)
```

- [ ] Удалить старые методы из workflowApi
- [ ] Добавить новые методы

### 2. Обновить useVideoWorkflow.ts

```typescript
const generateMutation = useMutation({
  mutationFn: (step) => workflowApi.generateStep(videoId, step),
  onSuccess: (response) => {
    queryClient.setQueryData(['video', videoId], response.data)
    queryClient.invalidateQueries(['video', videoId])
  }
})

const selectMutation = useMutation({
  mutationFn: (variantId) => workflowApi.selectVariant(videoId, variantId),
  onSuccess: (response) => {
    queryClient.setQueryData(['video', videoId], response.data)
    queryClient.invalidateQueries(['video', videoId])
  }
})
```

- [ ] Заменить startWorkflow на runWorkflow
- [ ] Обновить mutations с React Query cache invalidation
- [ ] Удалить approve/reject логику
- [ ] Добавить polling:
```typescript
refetchInterval: video?.status === 'in_progress' ? 3000 : false
```

### 3. Обновить InProgressView.tsx

```typescript
const WORKFLOW_STEPS = ['scenario', 'image', 'video', 'audio']

const getStepsForProject = (project) => {
  return project.audio_mode === 'none'
    ? WORKFLOW_STEPS.filter(s => s !== 'audio')
    : WORKFLOW_STEPS
}
```

- [ ] Удалить DISCOVER_STEPS и REMIX_STEPS
- [ ] Использовать единый WORKFLOW_STEPS
- [ ] Обновить AutoProgressView
- [ ] Добавить FailedStepCard с auto-retry (3 попытки)
- [ ] Добавить disable кнопок во время генерации

### 4. Обновить types/index.ts

- [ ] Обновить StepType: только `scenario`, `image`, `video`, `audio`
- [ ] Удалить interface WorkflowStep
- [ ] Удалить interface StepAttempt
- [ ] Удалить interface Variant
- [ ] Удалить interface WorkflowStepWithVariants
- [ ] Удалить StoryData, DescriptionData, PromptResponseData
- [ ] Обновить SystemPrompts: убрать story, description, prompt, adaptation

### 5. Удалить legacy hooks

- [ ] Удалить `src/hooks/useWorkflowV3.ts`

### 6. Создать VariantGallery.tsx

```tsx
// src/components/workflow/VariantGallery.tsx
interface Props {
  videoId: number
  step: StepType
  variants: StepHistory[]
  selectedId: number | null
  onSelect: (variantId: number) => void
}
```

- [ ] Создать VariantGallery.tsx
- [ ] Показывать все варианты из getHistory()
- [ ] Выделять selected вариант
- [ ] Кнопка "Regenerate" с feedback input
- [ ] Удалить `src/components/workflow/StepReview.tsx`

### 7. Удалить legacy компоненты

- [ ] Удалить `src/components/VideoWorkflowView.tsx`
- [ ] Удалить `src/components/StepCard.tsx`

### 8. Рефакторинг StepsList

- [ ] Удалить `src/components/video/StepsList.tsx`
- [ ] Переименовать `StepsListV3.tsx` → `StepsList.tsx`
- [ ] Обновить `buildStepsFromVideo()`: унифицировать 4 шага (убрать if remix)
- [ ] Обновить `InProgressView.tsx`: передавать `video` вместо `steps`
- [ ] Обновить `src/components/video/index.ts`: убрать StepsListV3

### 9. Обновить другие компоненты

- [ ] VideoDetail.tsx — убрать зависимости от workflow_steps

## Тесты

- [ ] `npm run build` — без ошибок
- [ ] `npm run lint` — без ошибок (или минимум)
- [ ] Ручной тест: создать видео AUTO → завершается
- [ ] Ручной тест: создать видео MANUAL → select работает

## E2E сценарии

### Discover AUTO
- [ ] Создать Discover проект → Video AUTO → Run → 4 шага → Completed

### Discover AUTO (без audio)
- [ ] audio_mode=none → 3 шага (SCENARIO → IMAGE → VIDEO) → Completed

### Discover MANUAL
- [ ] Generate SCENARIO → Select (auto-continue)
- [ ] Generate IMAGE (3 варианта) → Select (auto-continue)
- [ ] Generate VIDEO → Select (auto-continue)
- [ ] Generate AUDIO → Select → Completed

### Remix AUTO
- [ ] Создать Remix проект → Video AUTO → Run → Completed
- [ ] Проверить что SCENARIO заполняет переменные

### Выбор из истории
- [ ] Generate IMAGE_1 → VIDEO_1
- [ ] Generate IMAGE_2 → VIDEO_2
- [ ] Открыть галерею IMAGE → выбрать IMAGE_1
- [ ] Generate VIDEO_3 (от IMAGE_1)

### Remix MANUAL
- [ ] Generate SCENARIO (заполняет шаблон) → Select (auto-continue)
- [ ] Generate IMAGE (3 варианта) → Select (auto-continue)
- [ ] Generate VIDEO → Select (auto-continue)
- [ ] Generate AUDIO → Select → Completed

## Definition of Done

- [ ] `npm run build` проходит
- [ ] Все E2E сценарии работают
- [ ] Legacy компоненты удалены
- [ ] VariantGallery создан и работает

# Session 2026-02-04

## Что сделано

### T30 — Batch music pipeline (завершено)
- Автогенерация музыки для template-проектов: один трек Lyria2 на батч, лучший хук по энергии, мёрж со всеми видео
- Music prompt авто-генерируется из контекста шаблона, пользователь может редактировать в Configure → Step 5
- Новые статусы: `generating_audio`, `merging_audio`
- Протестировано e2e — работает

### История генераций на вкладке Generate
- Секция History с аккордеоном завершённых батчей
- Каждый батч показывает модерационную статистику: thumbs up/down/regen/pending
- Раскрывается — видно каждую генерацию с вариантом и статусом модерации
- Backend: `moderation_status` поле в GenerationResponse (batch-загрузка из approved_generations + rejection_archive)

### Fill schedule mode
- Убран опасный "All unused" режим из GenerationPanel
- Заменён на "Fill schedule" — авто-рассчитывает `needed = slots - approved - review - generating`
- Дефолтный режим если расписание настроено
- Под капотом отправляет `least_used` с exact count

## Технические изменения

### Backend (7 файлов + миграция)
- `models/template_settings.py` — `music_prompt` колонка
- `models/template_generation.py` — `audio_path`, `video_with_audio_path`, новые статусы
- `schemas/template.py` — `moderation_status`, audio поля, новые статусы
- `api/template.py` — `_generate_batch_music()`, `_generate_music_prompt_from_pipeline()`, moderation enrichment в list_generations
- `api/moderation.py`, `api/publishing_schedule.py` — prefer video_with_audio_path
- `services/template_generation_service.py` — `_step_merge_audio()`
- Миграция: `b8e7620999ad_add_music_to_template_pipeline.py`

### Frontend (6 файлов)
- `types/index.ts` — `moderation_status`, audio поля, новые статусы
- `BatchProgress.tsx` — аккордеон истории с модерацией, failed singles логика
- `GenerationPanel.tsx` — Fill schedule mode вместо All unused
- `GenerateScreen.tsx` — заголовок "Generations"
- `ConfigureSection.tsx` — Step 5 "Music" с textarea
- `GenerationsList.tsx` — prefer merged video

## Коммит
```
1a29e69 feat: T30 — batch music pipeline + generation history + fill schedule mode
14 files changed, 652 insertions(+), 133 deletions(-)
Branch: feature/T30-template-batch-music → pushed to dev
```

## Next steps
- Проверить музыку на реальном батче (полный e2e с Lyria2)
- Батчи с failed навсегда застревают в active секции — возможно добавить "dismiss" или auto-archive

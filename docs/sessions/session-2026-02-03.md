# Session 2026-02-03

## Что сделано

### T27: Phase 3B — Batch Generation + Remove Ratings
- Batch generation endpoint (`POST /generate/batch`) с 3 режимами: all_unused, least_used, specific
- Генерации обрабатываются последовательно (защита от rate limits)
- Рейтинги убраны из UI и API (колонки в БД остались)
- BatchProgress компонент — компактный прогресс батчей вместо полной истории
- Миграция: batch_id на template_generations

### T28: Tab Restructure
- 4 вкладки вместо 3: Dashboard / Generate / Review / Details
- Generate: Run Panel + BatchProgress (прогресс-бары, retry для failed)
- Details: Project settings (name, description, workspace) + ConfigureSection (5 шагов) с единым StickySaveBar
- PipelineScreen удалён
- Backward compat: `?screen=pipeline` → `?screen=generate`

### Pre-generate metadata при создании видео
- Publishing metadata (title, description, hashtags) генерируется сразу после создания видео
- Кеш на `TemplateGeneration.publishing_metadata` (JSON column)
- ReviewScreen показывает мету мгновенно — без прелоадера и LLM вызова
- Fallback для старых генераций: если кеша нет → вызов API как раньше
- Approve endpoint использует кешированную мету как default
- Миграция: `publishing_metadata` на template_generations

### Мелкие фиксы
- Publishing toggle: "Pause" → "Active" (зелёный = активен, серый = пауза)
- Variants endpoint: limit поднят с 100 до 500

## Технические изменения

### Backend
- `backend/app/models/template_generation.py` — batch_id, publishing_metadata fields
- `backend/alembic/versions/134464a3e21d_*.py` — миграция batch_id
- `backend/alembic/versions/eed2be6a0ed8_*.py` — миграция publishing_metadata
- `backend/app/schemas/template.py` — BatchGenerateRequest/Response, убраны рейтинги
- `backend/app/schemas/moderation.py` — publishing_metadata в ModerationQueueItem
- `backend/app/api/template.py` — POST /generate/batch, убран PATCH .../rating, limit 500
- `backend/app/api/moderation.py` — кеш меты в pre-generate и approve
- `backend/app/services/template_generation_service.py` — `_generate_metadata()` после completion

### Frontend
- Новые: BatchProgress.tsx, GenerateScreen.tsx, DetailsScreen.tsx
- Удалён: PipelineScreen.tsx
- Переписаны: GenerationPanel.tsx (batch UI), GenerationsList.tsx (batch grouping)
- Обновлены: TemplateProjectView (4 tabs), ConfigureSection (project settings), DashboardScreen, ReviewScreen (cached metadata), PipelineFunnel, DistributionStep, TemplateSettingsForm
- types/index.ts, api.ts — batch types, publishing_metadata в ModerationQueueItem

### Актуализация задач
- T19, T19.5, T20, T21, T24, T27, T28 → `tasks/done/`

## Commits
- `3790365` feat: T27+T28 — Batch generation, tab restructure, remove ratings
- (uncommitted) feat: Pre-generate publishing metadata at video completion

## Next steps
- Закоммитить metadata pre-generation + актуализацию доков
- Deploy и проверить на test сервере
- T23: Auto-Generation (связать batch count с расписанием)

# Session 2026-02-11

## Что сделано
- **T48: Dashboard Analytics Summary** — кросс-проектная аналитика на главной
  - Summary cards: Total Published, Total Views, Avg Views/Video, Publishing Cadence (actual vs target)
  - Sortable таблица по проектам (6 колонок, клик → переход в проект)
  - Publishing Health: цветовые индикаторы (green ≥7d, yellow ≥3d, red <3d), pending moderation, cadence
  - Backend: 4 batch-запроса (no N+1), health logic учитывает paused/disabled/no-config
  - PM + Tech Lead приёмка: ACCEPTED

- **Timezone bug fix** — timezone selection сбрасывался на UTC
  - Root cause: `ProjectBase` schema не включала `timezone` поле
  - Fix: `timezone: str = "UTC"` в `ProjectBase`

- **T49: Locale datetime formatting** — все 16 компонентов переведены на `formatDate`/`formatRelativeDate` из `@/utils/date`

- **T51: Queue management** — управление очередью публикаций в календаре
  - Backend: `PUT /reorder` + `POST /{item_id}/return-to-moderation` с валидацией дубликатов и sync metadata
  - Calendar view: DnD перетаскивание (flex-wrap layout) + return-to-moderation hover-кнопка
  - `ConfirmDialog` — единый компонент вместо нативных `confirm()` (4 места: calendar return, batch cancel)
  - `PublishingQueueView` удалён (Calendar/Queue toggle убран) — только календарь
  - `@hello-pangea/dnd` добавлен как dependency

## Технические изменения
- `backend/app/api/publishing_schedule.py` — 2 новых endpoint (reorder, return-to-moderation)
- `backend/app/schemas/publishing.py` — 3 новых schema
- `frontend/src/components/ui/ConfirmDialog.tsx` — NEW reusable component
- `frontend/src/components/template/PublishingScheduleView.tsx` — DnD + return button
- `frontend/src/components/template/BatchProgress.tsx` — CancelConfirmModal → ConfirmDialog
- `frontend/src/components/template/DashboardScreen.tsx` — убран Queue toggle
- `frontend/src/components/template/PublishingQueueView.tsx` — DELETED
- `frontend/src/services/api.ts` — returnToModeration, reorderQueue methods

## Next steps
- T45-T47 + T39/T40 — задачи месяца 1
- Заменить оставшиеся 13 нативных `confirm()` на ConfirmDialog (отдельная задача)
- Деплой T49+T51 через Coolify
